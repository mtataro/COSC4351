function calculate(){

   let enteredNum = prompt(); // Code will be tested with other values as well
   
   while (enteredNum < 60) {
      enteredNum *= 3;
      console.log(enteredNum);
   }

   
}
